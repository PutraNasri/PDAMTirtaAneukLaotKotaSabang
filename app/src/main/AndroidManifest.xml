<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.byonge.pdamtirtaaneuklaotkotasabang">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Keluhan"
            android:windowSoftInputMode="stateHidden" />
        <activity android:name=".tentang" />
        <activity android:name=".isi_berita" />
        <activity android:name=".pelanggan" />
        <activity
            android:name=".pasang_baru"
            android:windowSoftInputMode="stateHidden" />
        <activity android:name=".jadwal" />
        <activity android:name=".view_jadwal" />
        <activity android:name=".list_pasang_baru" />
        <activity android:name=".view_image"></activity>
    </application>

</manifest>